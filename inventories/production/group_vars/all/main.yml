env: production

dokku_hostname: supergood.cloud

dokku_users:
  - name: Nick
    username: nick
    ssh_key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"

apps:
  - supergood-reads
  - django-vue-experiments
