# These vars are shared among all (non-localhost) groups in all env inventories.

dokku_hostname_base: supergoodapp.site

dokku_users:
  - name: Nick
    username: nick
    ssh_key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
